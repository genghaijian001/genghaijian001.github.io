# site1

Standalone static personal homepage inspired by the plain structure of classic academic homepages.

This folder is intentionally independent. It does not import parent components, CSS, configuration, or routes.

## Run

From this folder:

```powershell
python -m http.server 3001
```

Then open:

```text
http://localhost:3001
```
